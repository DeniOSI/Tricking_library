name: build_my_app

on: push

jobs:
  build:
    name: Build our App
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v2
       - name: Current Directory
        run : pwd
       - name: File list
        run : ls